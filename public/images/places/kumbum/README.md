Add the author’s Kumbum Monastery cover image here as `cover.jpg` and gallery images in `gallery/`.
Register every published file in `data/places.ts`; files are not discovered automatically because alt text, credit, context, and rights must be explicit.
